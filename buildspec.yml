version: 0.2

phases:
    pre_build:
        commands:
            - echo logging in to AWS ECR...
            - $(aws ecr get-login --no-include-email --region us-east-1)
            - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
            - IMAGE_TAG=${COMMIT_HASH:=latest}
 
    build:
        commands:
            - echo build Docker image on `date`
            - docker build -t $REARC_QUEST_TERRAFORM_REPO:latest .
            - docker tag $REARC_QUEST_TERRAFORM_REPO:latest $REARC_QUEST_TERRAFORM_REPO:$IMAGE_TAG

    post_build:
        commands:
            - echo build Docker image complete `date`
            - echo push latest Docker images to ECR...
            - docker push $REARC_QUEST_TERRAFORM_REPO:latest
            - docker push $REARC_QUEST_TERRAFORM_REPO:$IMAGE_TAG

# phases:
#     pre_build:
#         commands:
#             - echo logging in to AWS ECR...
#             - $(aws ecr get-login --no-include-email --region us-east-1)
 
#     build:
#         commands:
#             - echo build Docker image on `date`
#             - docker build -t rearc-quest-terraform:latest .
#             - docker tag rearc-quest-terraform:latest $REARC-QUEST-TERRAFORM-REPO/rearc-quest-terraform:latest

#     post_build:
#         commands:
#             - echo build Docker image complete `date`
#             - echo push latest Docker images to ECR...
#             - docker push $REARC-QUEST-TERRAFORM-REPO/rearc-quest-terraform:latest

# phases:
#   build:
#     commands:
#       - echo Build started on `date`

#   post_build:
#     commands:
#       - echo Build completed on `date`

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"
  #secrets-manager:
     # key: secret-id:json-key:version-stage:version-id
     # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
     # - variable
     # - variable
  #git-credential-helper: yes
#batch:
  #fast-fail: true
  #build-list:
  #build-matrix:
  #build-graph:
# phases:
  #install:
    #If you use the Ubuntu standard image 2.0 or later, you must specify runtime-versions.
    #If you specify runtime-versions and use an image other than Ubuntu standard image 2.0, the build fails.
    #runtime-versions:
      # name: version
      # name: version
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
#   build:
#     commands:
      # - command
      # - command
  #post_build:
    #commands:
      # - command
      # - command
#reports:
  #report-name-or-arn:
    #files:
      # - location
      # - location
    #base-directory: location
    #discard-paths: yes
    #file-format: JunitXml | CucumberJson
#artifacts:
  #files:
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths